[project]
name = "cat-llm"
dynamic = ["version", "description", "authors", "readme", "license"]
requires-python = ">=3.12"

[build-system]
requires = ["maturin>=1.11,<2.0"]
build-backend = "maturin"

[tool.maturin]
bindings = "bin"
strip = true
